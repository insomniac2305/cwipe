export const publicRoutes = ["/"];

export const authRoutes = ["/login"];

export const apiAuthPrefix = "/api/auth";

export const apiCronPrefix = "/api/cron";

export const DEFAULT_LOGIN_REDIRECT = "/match";
